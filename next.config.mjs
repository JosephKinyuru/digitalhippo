/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'digitalhippo-production-4d9e.up.railway.app',
        ],
    },
};

export default nextConfig;
